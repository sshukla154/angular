// Declare function in JS
let log = function (message) {
    console.log(message);
}

// Declare function in TS
let logUsingArrowFunction = (message) => {
    console.log(message);
}

let logUsingArrowFunctionWithoutParamater = () => console.log();
